services:
  ai-backend:
    build:
      context: ./backend/ai-management
      dockerfile: Dockerfile
    volumes:
      - ./backend/ai-management:/app
    ports:
      - "5001:5001"
    environment:
      - PYTHONUNBUFFERED=1

  #frontend:
  #  build:
  #    context: ./frontend
  #    dockerfile: Dockerfile
  #  volumes:
  #    - ./frontend:/app
  #  ports:
  #    - "3000:3000"
  #  environment:
  #    - CHOKIDAR_USEPOLLING=true

  # Uncomment if you need a database
  # db:
  #   image: postgres:15
  #   environment:
  #     POSTGRES_USER: user
  #     POSTGRES_PASSWORD: password
  #     POSTGRES_DB: parkingdb
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - db_data:/var/lib/postgresql/data

# Uncomment if using a database
# volumes:
#   db_data:
